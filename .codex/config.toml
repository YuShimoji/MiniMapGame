# Minimal Codex project config scaffold.
# Keep this file short and project-specific.
# Canonical behavioral rules live in docs/ai/AGENT_RULES.md.
# Thread/runtime settings are intentionally inherited.
